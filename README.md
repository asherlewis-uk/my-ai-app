# Repository Status

This repository is not currently aligned with the locked Phase 1 product spec referenced by `docs/INDEX.md`, `docs/implementation-plan.md`, and `docs/tech-stack.md`.

## Current state

- The `docs/` directory is missing, so the locked source of truth cannot be verified locally.
- The existing code is a scaffold for a different product: a household-aware personal AI iOS app backed by Supabase and Ollama.
- Until the locked docs are restored, this repository should not be treated as Phase 1 complete or Phase 2 ready.

## Audit note

The current implementation uses conversations, profiles, memories, media analysis, and household data models. Those concepts need to be revalidated against the missing locked docs before further implementation continues.
